<%= form_for(@next_pick_number) do |f| %>
  <% if @next_pick_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@next_pick_number.errors.count, "error") %> prohibited this next_pick_number from being saved:</h2>

      <ul>
      <% @next_pick_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
